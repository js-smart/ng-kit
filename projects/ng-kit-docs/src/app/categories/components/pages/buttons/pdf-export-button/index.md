A button for exporting data to PDF format. The PDF Export Button is a standalone button component styled for PDF export functionality with a red background.

## Usage
```html
<pdf-export-button></pdf-export-button>
```

## API Reference
The PDF Export Button is a standalone component and does not extend the Base Button. It is a simple button with fixed styling and text.

This component does not expose any configurable inputs or outputs.
